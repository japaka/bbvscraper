{
  "name": "bbv-scraper",
  "version": "0.0.0",
  "description": "Scrapes the bbv-website for stats",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "japaka",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.20.0",
    "mkdirp": "^0.5.1",
    "request": "^2.69.0"
  }
}
